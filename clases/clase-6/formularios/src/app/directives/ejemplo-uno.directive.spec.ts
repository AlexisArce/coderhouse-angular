import { EjemploUnoDirective } from './ejemplo-uno.directive';

describe('EjemploUnoDirective', () => {
  it('should create an instance', () => {
    const directive = new EjemploUnoDirective();
    expect(directive).toBeTruthy();
  });
});
